version: '3.8'
services:
  app:
    image: nginx:alpine
    container_name: limited-app
    ports: ["9000:9000"]
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256m
    restart: always
    